package Consumidor_Productor;

public class Consumidor implements Runnable {

	private Productos productos;

	@Override
	public void run() {

		while (true) {

			if (productos.getProductos().size() > 0) {
				int cant = productos.getProductos().size();
				try {
					Thread.sleep(1000);
				} catch (InterruptedException e) {
					e.printStackTrace();
				}
				
					if (productos.getProductos().size() == cant) {
						productos.comprar();
					}
				

//			} else {
//				try {
//					System.out.println(Thread.currentThread().getName() + " Esperando para comprar");
//					Thread.sleep(2000);
//				} catch (InterruptedException e) {
//					e.printStackTrace();
//				}
			}

		}
	}

	public Consumidor(Productos productos) {
		super();
		this.productos = productos;
	}
}
